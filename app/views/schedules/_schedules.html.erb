<div class="card mb-1">
  <div class="card-body">
    <p class="card-text"><%= l @schedule.created_at, format: '%Y年%-m月%-d日(%a)' %></p>
    <p class="card-text"><%= @schedule
        .title %></p>
    <p class="card-text"><small><%= simple_format @schedule.content %></small></p>
    <%= link_to("削除","@schedule/#{schedule.id}/destroy", {method: "post"},
  )%>
  </div>
</div>